node_modules/
dist/
data/
data-demo/
coverage/
.env

# User-specific config (contains private IPs / service URLs) — copy config.example.yaml instead
config.yaml

# Build artefacts
*.tar
output/kodi/*.zip

# Old Python prototype (superseded by Node.js rewrite)
pyproject.toml
requirements.txt

# Copilot conversation logs
.copilot/
